import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 15,
    marginLeft: 20,
  },
  title: {
    fontSize: 20,
    color: "white" || "#fff",
  },
  subtitle: {
    color: "#adadad",
    fontSize: 11,
  },
});
